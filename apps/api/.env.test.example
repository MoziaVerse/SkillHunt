# Template for apps/api/.env.test (gitignored).
# Bun auto-loads this when NODE_ENV=test (or via --env-file). Tests TRUNCATE
# tables freely — pointing at a separate DB keeps dev data safe.
PORT=3333
DATABASE_URL=postgres://postgres:postgres@localhost:36915/mozia_skillhub_test

# OIDC / better-auth left empty: auth.ts has graceful fallbacks under
# NODE_ENV=test so tests don't need real SSO config.
