{
  "name": "@mozia/skillhub-scripts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "bun --env-file=../apps/api/.env.test test",
    "import:builtin:local": "bun import-builtin-skills.ts",
    "sync:hdu-contest-tag": "bun --env-file=../apps/api/.env sync-hdu-contest-external-tag.ts",
    "sync:hdu-contest-tag:bridge": "bun --env-file=../apps/api/.env.testbridge sync-hdu-contest-external-tag.ts",
    "seed:owned": "bun --env-file=../apps/api/.env seed-owned.ts",
    "seed:referenced": "bun --env-file=../apps/api/.env seed-referenced.ts",
    "seed:owned:bridge": "bun --env-file=../apps/api/.env.testbridge seed-owned.ts",
    "seed:referenced:bridge": "bun --env-file=../apps/api/.env.testbridge seed-referenced.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
